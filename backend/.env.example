# Optional: leave unset to use local SQLite (expenses.db)
# Railway sets this automatically if you attach a Postgres plugin
DATABASE_URL=

# Comma-separated list of allowed frontend origins, or "*" for any (dev only)
CORS_ORIGINS=*
